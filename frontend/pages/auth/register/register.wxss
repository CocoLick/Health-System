/* 与登录页共用基座，见 login.wxss；此处仅补注册表单项 */
@import '../login/login.wxss';

.auth-card-inner--tall {
  padding-top: 36rpx;
  padding-bottom: 32rpx;
}

.picker-view {
  flex: 1;
  min-width: 0;
  font-size: 28rpx;
  color: var(--c-text);
  min-height: 48rpx;
  line-height: 48rpx;
}

.picker-placeholder {
  color: var(--c-text-3);
  font-size: 28rpx;
}
